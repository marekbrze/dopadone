linters:
  enable:
    - gofmt
    - goimports
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - typecheck
    - gosimple
    - goconst
    - gocyclo
    - dupl

linters-settings:
  gocyclo:
    min-complexity: 20
  goconst:
    min-len: 3
    min-occurrences: 3
  dupl:
    threshold: 150

run:
  timeout: 5m
  skip-dirs:
    - vendor
    - testdata

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - dupl
        - goconst
